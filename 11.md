# 第11回 線形リスト（その2　双方向連結リスト）

## 講義資料

### 基本形と使用例

```python
class Node:
  def __init__(self, data, prev=None, next=None):
    self.data = data
    self.prev = prev
    self.next = next


class DoublyLinkedList:
  def __init__(self):
    self.head = None
    self.tail = None

  def add_first(self, data):
    node = Node(data, next=self.head)
    if self.head:
      self.head.prev = node
    else:
      self.tail = node
    self.head = node


# 使用例
a = DoublyLinkedList()
a.add_first(20)
a.add_first(10)
a.add_first(30)

# 前から処理する
print(a.head.data, end=' ')
print(a.head.next.data, end=' ')
print(a.head.next.next.data)

p = a.head
while p:
  print(p.data, end=' ')
  p = p.next
print()

# 後ろから処理する
print(a.tail.data, end=' ')
print(a.tail.prev.data, end=' ')
print(a.tail.prev.prev.data)

p = a.tail
while p:
  print(p.data, end=' ')
  p = p.prev
```

## プレゼン課題

次の実装を使って解答してください．ただし，`LinkedList` を使うときは，`Node.prev` を使ってはいけません．

```python
class Node:
  def __init__(self, data, prev=None, next=None):
    self.data = data
    self.prev = prev
    self.next = next


class LinkedList:
  def __init__(self):
    self.head = None

  def add_first(self, data):
    self.head = Node(data, next=self.head)

  def find(self, data):
    p = self.head
    while p:
      if p.data == data:
        return p
      p = p.next
    return None


class DoublyLinkedList:
  def __init__(self):
    self.head = None
    self.tail = None

  def add_first(self, data):
    node = Node(data, next=self.head)
    if self.head:
      self.head.prev = node
    else:
      self.tail = node
    self.head = node

  def find(self, data):
    p = self.head
    while p:
      if p.data == data:
        return p
      p = p.next
    return None
```

### 1

単方向連結リストの要素を反転させる方法を説明し，メソッド `reverse` で実装してください．計算量についても説明してください．

### 2

双方向連結リストの要素を反転させる方法を説明し，メソッド `reverse` で実装してください．計算量についても説明してください．

### 3

この問題は，第10回のプレゼン課題3に対応しています．

双方向連結リストにおいて，指定したデータ `data` をもつ要素 `p` をメソッド `find` で発見したとします．`p` の次にデータ `new_data` をもつ新しい要素を挿入する方法を説明し，メソッド `insert` で実装してください．挿入の計算量についても説明してください．発見しない場合についての考慮は不要です．実行例を示します．

```python
a = DoublyLinkedList()
a.add_first(20)
a.add_first(10)
a.add_first(30)

p = a.find(10)
a.insert(p, 40)

p = a.head
while p:
  print(p.data)
  p = p.next
```

### 4

この問題は，第10回のプレゼン課題4に対応しています．

双方向連結リストにおいて，指定したデータ `data` をもつ要素 `p` をメソッド `find` で発見したとします．`p` を削除する方法を説明し，メソッド`remove` で実装してください．削除の計算量についても説明してください．発見しない場合についての考慮は不要です．実行例を示します．

```python
a = DoublyLinkedList()
a.add_first(20)
a.add_first(10)
a.add_first(30)

p = a.find(10)
a.remove(p)

p = a.head
while p:
  print(p.data)
  p = p.next
```

