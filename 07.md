# 第7回 ソート（その1）

## 講義資料

### 準備

#### ラムダ

```python
print((lambda x: x + 10)(3))
print((lambda x: x[0])([10, 20]))
```

#### タプルの比較

```python
print((1, 2) < (2, 2))
print((1, 2) < (1, 3))
print((1, "a") < (2, "a"))
print((1, "a") < (1, "b"))
```

#### 関数 sorted とメソッド sort

```python
a = [82, 75, 91, 75]
b = sorted(a)

print(b)
print(a)
```

```python
a.sort()
print(a)
```

##### データが複数のシーケンスで管理されている場合

```python
names = ["X", "Z", "W", "Y"]
scores = [82, 75, 91, 75]

# score, nameの順に比較する
sorted(zip(scores, names))
# 「scoreでソート」としては安定でない
```

```python
# scoreでソート（安定）
sorted(zip(scores, names), key=lambda x:x[0])
```

```python
# 順番だけ先に求めて，その順番で取り出す
import numpy as np

idx = np.argsort(scores)
print(idx)
print(np.array(scores)[idx])
print(np.array(names)[idx])
```

##### 独自オブジェクトのシーケンスのソート

- 方法1：`key` を指定する．
- 方法2：比較方法を定義する．

### バブルソート

- [Obama on sorting 1M integers: Bubble sort the wrong way to go ](https://www.righto.com/2012/11/obama-on-sorting-1m-integers-bubble.html)


```python
# 参考書のコード
!wget -q https://www.bohyoh.com/Books/NewMeikaiPythonAlgorithm_2nd/NewMeikaiPythonAlgorithm2ndSource.zip
!unzip -q NewMeikaiPythonAlgorithm2ndSource.zip
!cat chap06/bubble_sort1.py
```

```python
# 参考書のコードの使用例
from chap06.bubble_sort1 import bubble_sort

a = [82, 75, 91, 75]
bubble_sort(a)
print(a)
```

## プレゼン課題

課題1と2では，次のコードを使用します．

```python
from dataclasses import dataclass

@dataclass
class Student:
    name: str
    score: int


students = [
    Student("X", 82),
    Student("Z", 75),
    Student("W", 91),
    Student("Y", 75),
]
```

### 1

リスト `students` を `score` の昇順（小さい順）に Python の `sorted` でソートする方法を説明して，実際にソートしてください．ただし，クラス `Student` の定義を変更してはいけません．また，ソートは安定でなければなりません．

### 2

リスト `students` を `score` の昇順（小さい順）にソートする方法を説明して，実際にソートしてください．クラス `Student` の定義は変更してかまいませんが，ソートの実行方法は `sorted(students)` で，これを変更してはいけません．また，ソートは安定でなければなりません．

### 3

次のコードで作られる，0 以上 100 以下の乱数 100 個からなるリスト `xs` を，参考書の chap06/bubble_sort1.py で定義されている関数 `bubble_sort` で昇順にソートして，比較 `>` が行われる回数を数えてください．

```python
import random

xs = [random.randint(0, 100) for _ in range(100)]
```

### 4

0 以上 100 以下の乱数 100 個からなるリストを，メソッド `sort` で昇順にソートして，比較 `<` が行われる回数を数えてください．必要なら，整数をラップするクラスを定義してかまいません．
