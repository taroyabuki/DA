# 第2回 データ構造と配列

- 配列
- インデックスとスライス
- 走査（traverse）のパターン
- 最大値
- 検証（テストと静的解析）

## プレゼン課題

### 1

次のコードでは，リスト `b` を変更すると，リスト `a` も変更されます．

```python
a = [1, 2, 3, 4]
b = a
b[0] = 100
a
```

リスト `b` を変更してもリスト `a` が変更されないようにする方法を複数紹介してください．ただし，リスト `b` の変更した要素以外の要素はリスト `a` の要素と等しくなければなりません．

### 2

リスト `a` の要素の並びを反転させる方法には，次のようなものがあります．

1. `a.reverse()`
2. `b = list(reversed(a))`
3. `b = a[::-1]`

これらの違いを説明してください．

### 3

リストの要素の並びを反転させる関数を実装してください．ただし，前問で挙げた方法は使わず，`for` を使ってください．

### 4

最大値を求める関数 `max_of` を実装し，テストしています．

```python
%%writefile max.py
def max_of(a):
    return max(a)

def test_max_of():
    assert max_of([20, 10, 30]) == 30
    assert max_of([5]) == 5
    assert max_of([1, 3, 3, 2]) == 3

test_max_of()
print('テスト成功')
```

`!python max.py` で動くのですが，次のように単に使う場合にもテストが実行されてしまう問題があります．

```python
import max

a = [1, 2, 3, 4]
print(max.max_of(a))
```

問題の解決法を説明して，実装してください．

ヒント：インポートをやり直すときは，次のコードを実行してください．

```python
import sys
del sys.modules['max']
```

### 5

準備：この課題で使う `pyright` をインストールします．

```
!python -m pip install pyright
```

リストを反転させる関数 `reverse_in_place` を作っています．

```python
%%writefile rev.py
from collections.abc import Sequence

def reverse_in_place(a: Sequence) -> Sequence:
  a.reverse()
  return a

assert reverse_in_place([1, 2, 3]) == [3, 2, 1]
assert reverse_in_place([1, 2, 2, 3]) == [3, 2, 2, 1]
```

`!python rev.py` でテストすると成功しますが，次のように `pyright` でチェックするとエラーがあることがわかります．そのエラーについて説明して，エラーを修正してください．

```
!pyright rev.py
```