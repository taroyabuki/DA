# 第9回 文字列探索

## 講義資料

```
# 参考書のコード
!wget -q https://www.bohyoh.com/Books/NewMeikaiPythonAlgorithm_2nd/NewMeikaiPythonAlgorithm2ndSource.zip
!unzip -q NewMeikaiPythonAlgorithm2ndSource.zip
```

```
# 参考書の 力まかせ 法
!cat chap07/kmp_match.py
```

```python
# 力まかせ法の使用例
from chap07.bf_match import bf_match

print(bf_match('ZABCABXA', 'ABCABD'))

print(bf_match('PZABCABDX', 'ABCABD'))
```

```
# 参考書の KMP 法
!cat chap07/kmp_match.py
```

```python
# KMP 法の使用例
from chap07.kmp_match import kmp_match

print(kmp_match('ZABCABXA', 'ABCABD'))

print(kmp_match('PZABCABDX', 'ABCABD'))
```

## プレゼン課題

### 1

Pythonの文字列がもつメソッド `find` と `index` の違いを，コードを使って説明してください．

### 2

「すもももももももものうち」に「もも」が何回現れるかを，Pythonの文字列がもつメソッド `find` あるいは `index` を使って数えてください．

### 3

KMP 法で `aaaaa` から `aaab` を探索する場合のスキップテーブルがどうなるかを説明してください．ヒント：参考書のコードを実行して `skip` の内容を表示させてみるとよいでしょう．

### 4

参考書の力まかせ法と KMP 法で，`ABABCDEFGHA` から `ABC` を探索する場合の，`txt[pt] == pat[pp]` の評価回数を比較してください．

### ♠5

括弧の対応を調べる次のプログラムを，スタックを使うものに書き直してください．

```python
import regex

pattern = regex.compile(r"\((?:[^()]|(?0))*\)")

tests = [
    "(abc)",
    "(a(b)c)",
    "((abc))",
    "(a(b(c)d)e)",
    "(a(b)",
]

for s in tests:
    print(s, bool(pattern.fullmatch(s)))
```