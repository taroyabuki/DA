# 第3回 探索1

- Python
    - 浅いコピーと深いコピー
    - ミュータブル（変更可能）とイミュータブル（変更不能）
    - 型ヒント（int, float, Sequence, MutableSequence）
- 探索
    - 線形探索
        - 時間計算量は $O(n)$
        - 番兵法
    - 2分探索（次回）
    - ハッシュ法（次回）

## 補足

### 浅いコピーと深いコピー

```python
import copy

a = [[1, 2], 3]
b = a
c = a.copy()
d = copy.deepcopy(a)

a[0][0] = 10
a[1] = 30

print("a =", a)
print("b =", b)
print("c =", c)
print("d =", d)
```

### ミュータブルなリスト，イミュータブルなタプル

```python
a = [1, 2, 3]
b = (1, 2, 3)

a[0] = 10
print(a)

b[0] = 10 # エラー
print(b)
```

### 型ヒント

```
!pip -q install pyright
```

```python
%%writefile f.py
def f(x):
  return 10*x

print(f(3))
print(f('abc')) # こういう使い方を排除したいとする．
```

```
!python f.py # 動く

!pyright f.py # 静的解析もパス
```

```python
%%writefile f2.py
def f(x: int | float) -> int | float:
    return 10 * x

print(f(3))
print(f('abc'))
```

```
!python f2.py # 動く

!pyright f2.py # 静的解析でエラー
```

```python
%%writefile f3.py
from beartype import beartype

@beartype
def f(x: int | float) -> int | float:
    return 10 * x

print(f(3))
print(f('abc'))
```

```
!python f3.py # 実行時エラー
```

### 線形探索

#### Python

```python
a = [6, 4, 3, 2, 1, 2, 8] 

print(a.index(2)) # 3
#print(a.index(5)) # ValueError

try:
  print(a.index(5))
except:
  print(-1)
```

#### 自分で実装する

`while` を使う．

```python
def search1(a, key):
  i = 0
  while True:
    if i == len(a): return -1
    if a[i] == key: return i
    i += 1

a = [6, 4, 3, 2, 1, 2, 8]
print(search1(a, 2))
print(search1(a, 5))
```

`for x in a` を使う．

```python
def search2(a, key):
  for i in range(len(a)):
    if a[i] == key: return i
  return -1

a = [6, 4, 3, 2, 1, 2, 8]
print(search2(a, 2))
print(search2(a, 5))
```

`for i, x in enumerate(a)` を使う．

```python
def search3(a, key):
  for i, x in enumerate(a):
    if x == key: return i
  return -1

a = [6, 4, 3, 2, 1, 2, 8]
print(search3(a, 2))
print(search3(a, 5))
```

#### 番兵法

参考書の List 3.3

```
%%bash
wget -q https://www.bohyoh.com/Books/NewMeikaiPythonAlgorithm_2nd/NewMeikaiPythonAlgorithm2ndSource.zip
unzip -q NewMeikaiPythonAlgorithm2ndSource.zip
cat chap03/ssearch_sentinel.py
```

## プレゼン課題

### 1

次のコードにおいて，`a[0]`, `b[0]`, `c[0]`, `d[0]`が指すオブジェクトの同一性を調べてください．ヒント：`id`を使う．

```python
import copy

a = [[1, 2], 3]
b = a
c = a.copy()
d = copy.deepcopy(a)
```

### 2

「整数 $0, 1, 2, \dots, n$ をランダムシャッフルしてできるリスト `a` から，`0` を線形探索するのにかかる時間を計る」という実験を $10$ 回行って，平均時間を求めてください． $n$ が $100000$ から $1000000$ になると，平均時間はどうなりますか．

### 3

参考書の List 3-1 (chap03/ssearch_while.py) と List 3-2 (chap03/ssearch_for.py) の性能を比較してください．

### 4

参考書の List 3-3 (chap03/ssearch_sentinel.py) を `pyright` で静的解析してください．コードをどのように修正すればよいか説明してください．
