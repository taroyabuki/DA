# 第4回 探索2

- O記法
- 探索
    - 線形探索（前回）
        - 実用：`a.index(key)`
        - 学習用：chap03/search_for.py
    - 二分探索：時間計算量は $O(\log n)$
        - 実用：`bisect_left(a, key)`
        - 学習用：chap03/bsearch.py
    - ハッシュ法：時間計算量は $O(1)$
        - 実用：`set`
        - 学習用
            - チェイン法：chap03/chained_hash.py
            - オープンアドレス法：chap03/open_hash.py

## O記法

$n, \log n, n\log n, n^2$ の比較：https://www.wolframalpha.com/input?i=plot+n%2Clog+n%2Cn+log+n%2Cn%5E2+for+n%3D1..20

## 二分探索

ライブラリで二分探索

```python
from bisect import bisect_left

def bin_search_lib(a, key):
    i = bisect_left(a, key) # 要素を挿入できる場所
    if i != len(a) and a[i] == key: # 値の確認が必要
        return i
    return -1

a = [5, 7, 15,28, 29, 31, 39, 58, 68, 72, 95]
print(bin_search_lib(a, 39))
```

参考書のコード（実装の確認）

```bash
%%bash
wget -q https://www.bohyoh.com/Books/NewMeikaiPythonAlgorithm_2nd/NewMeikaiPythonAlgorithm2ndSource.zip
unzip -q NewMeikaiPythonAlgorithm2ndSource.zip
cat chap03/bsearch.py
```

```python
from chap03.bsearch import bin_search

a = [5, 7, 15,28, 29, 31, 39, 58, 68, 72, 95]
print(bin_search(a, 39))
```

## ハッシュ法

`set` の使い方

```python
a = {5, 6, 14, 20, 29, 34, 37, 51, 69, 75}

a.add(35)
a.remove(6)

print(51 in a)
print(6 in a)

print(a)
```

## プレゼン課題

### 1

$n$ が増加するときの， $n, \log n, n\log n, n^2$ の増加の仕方を，Pythonで可視化してください．

### 2

次の Python コードについて，`n` を 10, 100, 1000 と変化させたときに処理回数 `count` がどのように増えるかを調べて，表にまとめてください．さらに，`f1`，`f2`，`f3` の時間計算量をO記法を使って表してください．

```python
def f1(n):
    count = 0
    for i in range(n):
        count += 1
    return count


def f2(n):
    count = 0
    for i in range(n):
        for j in range(n):
            count += 1
    return count


def f3(n):
    count = 0
    x = 1
    while x < n:
        count += 1
        x *= 2
    return count
```

### 3

次の作業を行うためのPythonのコードを書いてください．探索はライブラリで行ってかまいません．

1. 0からNまでの整数の乱数をM個作り，配列Aとする（重複あり）．
2. 0からnまでの整数でAに属しているものの個数を，線形探索で1個ずつ調べて求める．
3. `B = sorted(A)` として整列してから，0からnまでの整数でBに属しているものの個数を，二分探索で1個ずつ調べて求める．

**手順2と手順3の結果は同じになるはです．**

手順2の方が早くなる(N, M, n)と，手順3の方が早くなる(N, M, n)を，それぞれ一組ずつ見つけて，その結果について考察してください．ヒント：N, M, nのうち2個は共通にしておくと考察しやすいでしょう．

### 4

次の作業を行うためのPythonのコードを書いてください．探索はライブラリで行ってかまいません．

1. 0からNまでの整数の乱数をM個作り，配列Aとする（重複あり）．
2. 0からnまでの整数でAに属しているものの個数を，線形探索で1個ずつ調べて求める．
3. `B = set(A)` として集合を作ってから，0からnまでの整数でBに属しているものの個数を，1個ずつ調べて求める．

**手順2と手順3の結果は同じになるはです．**

手順2の方が早くなる(N, M, n)と，手順3の方が早くなる(N, M, n)を，それぞれ一組ずつ見つけて，その結果について考察してください．ヒント：N, M, nのうち2個は共通にしておくと考察しやすいでしょう．
