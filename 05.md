# 第5回 スタックとキュー

- スタック
    - 実用：`collections.deque`
    - 学習用：chap04/fixed_stack.py
    - 学習用：chap04/stack.py（`collections.deque`のインタフェースをchap04/fixed_stack.pyに合わせたもの）
- キュー
    - 実用：`collections.deque`
    - 学習用：chap04/fixed_queue.py
    - 学習用：chap04/queue.py（`collections.deque`のインタフェースをchap04/fixed_queue.pyに合わせたもの）

## プレゼン課題

### 1

Python の `collections.deque` をスタックとして使って，次を比較して，結果について考察してください．ただし，スタックの top は deque の右端とし，push には `append()`，pop には `pop()` を使います．また，スタックを初期化する時間は測定に含めないこと．

1. 要素数が 100 個のスタックを用意する．「push と pop を 1 回ずつ実行する」処理を 10,000 回実行するのにかかる時間を測定する．
2. 要素数が 100 万個のスタックを用意する．「push と pop を 1 回ずつ実行する」処理を 10,000 回実行するのにかかる時間を測定する．

ヒント：Google Colabで試す場合は，`%%timeit` で時間を計るのが簡単です．最初の実験は次のように行うとよいでしょう．

```python
from collections import deque # deque をインポートしておく．
```

```python
%%timeit -n 1 -r 10 stack = deque(range(100))

# 「push と pop を 1 回ずつ実行する」処理を 10,000 回実行する
```

### 2

Python のリストをキューとして用いる，非効率な方法を示します．

```python
queue = []
queue.append(54)
queue.append(65)
queue.append(83)
print(queue)
print(queue.pop(0))
queue.append(79)
print(queue)
```

この方法がよくないことを，次の結果の比較によって説明してください．説明には，エンキューとデキューの時間計算量の見積りを含めてください．ただし，キューを初期化する時間は測定に含めないこと．

1. 要素数が 100 個のキューを用意する．「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行するのにかかる時間を測定する．
1. 要素数が 1万個のキューを用意する．「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行するのにかかる時間を測定する．

ヒント：Google Colabで試す場合は，`%%timeit` で時間を計るのが簡単です．最初の実験は次のように行うとよいでしょう．

```python
%%timeit -n 1 -r 10 queue = list(range(100))

# 「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行する
```

### 3

Python のリストをキューとして用いる，改善の余地がある方法を示します．話を簡単にするために，実装は必要最低限にしています．

```python
class FrontRearQueue:
    def __init__(self):
        self.data = []
        self.front = 0
        self.rear = 0

    def append(self, value): # 本来は enqueue
        self.data.append(value)
        self.rear += 1

    def popleft(self): # 本来は dequeue
        value = self.data[self.front]
        self.front += 1
        return value


queue = FrontRearQueue()
queue.append(54)
queue.append(65)
queue.append(83)
print(queue.popleft())
print(queue.popleft())
print(queue.popleft())
```

この方法について，次の結果の比較を踏まえて考察してください．

1. 要素数が 100 個のキューを用意する．「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行するのにかかる時間を測定する．リスト `data` の最終的な要素数を求める．
1. 要素数が 1万個のキューを用意する．「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行するのにかかる時間を測定する．リスト `data` の最終的な要素数を求める．

ヒント：Google Colabで試す場合は，`%%timeit` で時間を計るのが簡単です．最初の実験は次のように行うとよいでしょう．

```python
%%timeit -n 1 -r 10 queue = FrontRearQueue(); queue.data = list(range(100)); queue.rear = 100

# 「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行する
```

`%%timeit` は同じ処理を複数回実行します．`queue.data` の要素数は上のコードの実行後に 1 回だけ測定すればいいです．

### 4

Python のリストをリングバッファとして用いる方法を示します．話を簡単にするために，実装は必要最低限にしています．空のキューから要素を取り出したり，満杯のキューにデータを追加することは想定しません．

```python
class RingQueue:
    def __init__(self, capacity):
        self.data = [None] * capacity
        self.front = 0
        self.rear = 0

    def append(self, value): # 本来は enqueue
        self.data[self.rear] = value
        self.rear = (self.rear + 1) % len(self.data)

    def popleft(self): # 本来は dequeue
        value = self.data[self.front]
        self.front = (self.front + 1) % len(self.data)
        return value


queue = RingQueue(4)
queue.append(54)
queue.append(65)
queue.append(83)
print(queue.popleft())
print(queue.popleft())
print(queue.popleft())
```

この方法について，次の結果の比較を踏まえて考察してください．

1. 要素数が 100 個のキューを用意する．「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行するのにかかる時間を測定する．リスト `data` の最終的な要素数を求める．
1. 要素数が 1万個のキューを用意する．「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行するのにかかる時間を測定する．リスト `data` の最終的な要素数を求める．

ヒント：Google Colabで試す場合は，`%%timeit` で時間を計るのが簡単です．最初の実験は次のように行うとよいでしょう．

```python
%%timeit -n 1 -r 10 queue = RingQueue(101); queue.data = list(range(100)) + [None]; queue.rear = 100

# 「エンキューとデキューを 1 回ずつ実行する」処理を 10,000 回実行する
```

### 全体のまとめ

担当した問題によらず，次の表を完成させてください．ただし，初期状態の要素数を n，「エンキューとデキュー」または「push と pop」を繰り返す回数を k とし，n や k を使う数式を入れてください．

| 問題 | 処理時間は何に比例しそうか | 使用メモリは何に比例しそうか |
|---|---|---|
| 1 | |  |
| 2 | |  |
| 3 | |  |
| 4 | |  |
