# 第8回 ソート（その2）

## 講義資料

### 宿題（任意）

表を完成させて暗記する．

名前|平均時間|最悪時間|追加メモリ|安定性
--|--|--|--|--
挿入ソート|
クイックソート|
マージソート|
ヒープソート|
度数ソート|

```bash
# 参考書のコード
!wget -q https://www.bohyoh.com/Books/NewMeikaiPythonAlgorithm_2nd/NewMeikaiPythonAlgorithm2ndSource.zip
!unzip -q NewMeikaiPythonAlgorithm2ndSource.zip
```

### クイックソート

```python
#クイックソートもどき
def sort(A):
  if len(A) < 2: return A 

  p = A[-1]
  left   = [x for x in A if x <  p]
  middle = [x for x in A if x == p]
  right  = [x for x in A if x >  p]

  return sort(left) + middle + sort(right)

a = [82, 75, 91, 75]
sort(a)
```

```python
#参考書のクイックソート
!cat chap06/quick_sort1.py
```

```python
#ライブラリでクイックソート
import numpy as np

a = np.array([82, 75, 91, 75])
a.sort(kind='quicksort')
print(a)
```

### マージソート

```python
#参考書のマージソート
!cat chap06/merge_sort.py
```

```python
#ライブラリでマージソート？
import numpy as np

a = np.array([82, 75, 91, 75])
#安定だが，マージソートになるとは限らない
a.sort(kind='stable') # a.sort(kind='mergesort')としても同じ．
print(a)
```

### ヒープソート

```python
#参考書のヒープソート
!cat chap06/heap_sort.py
```

```python
#ライブラリでヒープソート（heapqを活用する）
#参考書のchap06/heap_sort_heapq.pyは，O(n)のheapifyでなく，O(log n)のheappushをn回使っているから，おそらく遅い．
import heapq

def heapsort(A):
    h = list(A)
    heapq.heapify(h)
    return [heapq.heappop(h) for _ in range(len(h))]

a = [82, 75, 91, 75]
print(heapsort(a))
```

### 度数ソート

```python
#参考書の度数ソート
!cat chap06/counting_sort.py
```

## プレゼン課題

### 1

クイックソートは安定ではありませんが，追加のメモリを許容して安定にすることができます．次のコードの `a` を，先頭要素だけをキーにして，クイックソートで安定にソートする方法を説明してから，実際にソートしてください．ソートの実行部分のコードを変更してはいけません．

```python
import numpy as np

a = np.array([(82, 'A'), (75, 'B'), (91, 'C'), (75, 'D')])

# ここでaを加工してもよい

a.sort(kind='quicksort')
print(a)
```

### 2

$n=10^6$ とします． $0$ 以上 $n$ 未満の整数の乱数 $n$ 個からなる Python のリスト `a`から，小さい順に $10$ 個取り出したいとします．次の2通りの方法の，実行時間を比較して，結果について考察してください．

1. `a.sort` でソートして，先頭 $10$ 個を取り出す．
2. `heapq` を使って `a` をヒープにしてから， $10$ 個取り出す．

### 3

$0$ 以上 $255$ 以下の整数の乱数 $10^6$ 個からなる Python のリスト `a` を，次の2通りの方法でソートするのにかかる時間を測定し，結果について考察してください．度数ソートは，参考書の chap06/counting_sort.py を使ってかまいません．補足：PyPy を使ってはいけません（問題4で PyPy を使うから）．

1. `a.sort()` でソートする．
2. 度数ソートでソートする．

### 4

問題3を PyPy で試してください．

ヒント：Google Colab で PyPy を使う方法を示します．

1. PyPy をインストールします．

```
!apt-get install -y pypy3 > /dev/null
```

Python のコードをファイルに書きます．

```
%%writefile test.py
print('hello, world')
```

実行します．

```
!pypy3 test.py
```

### ♠5

32ビット非負整数の乱数100万個からなる Pythonのリスト `a` を，次の2通りの方法でソートする実行時間を比較して，結果について考察してください．PyPy を使ってもかまいません．

1. `a.sort()` でソートする．
2. 基数ソート（8ビットごとに区切って，下位ビットから4回度数ソートを実行）でソートする．
