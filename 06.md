# 第6回 再帰的アルゴリズム

再帰の考え方を例を通じて学ぶ

- 階乗
- GCD
- 二分探索
- 部分集合の列挙
- サイコロの合計の数え上げ
- 演習1：各桁の和
- 演習2：要素の計測
- 演習3：回分判定
- 演習4：文字列の列挙

## 講義

### 階乗

#### 実用的な方法

```python
import math

print(math.factorial(4))
```

#### 再帰を使う方法

```python
def fact(n):
    if n == 0:
        return 1
    return n * fact(n - 1)


print(fact(4))
```


### GCD / ユークリッドの互除法

#### 実用的な方法

```python
import math

print(math.gcd(84, 30))
```

#### 再帰を使う方法

```python
def gcd(a, b):
    if b == 0:
        return a
    return gcd(b, a % b)


print(gcd(84, 30))
```


### 二分探索

#### 実用的な方法

```python
from bisect import bisect_left

def binary_search(a, x):
    i = bisect_left(a, x)

    if i < len(a) and a[i] == x:
        return i

    return -1


a = [2, 5, 8, 12, 16, 23, 38]

print(binary_search(a, 16))  # 4
print(binary_search(a, 7))   # -1
```

#### 再帰を使わない方法

```python
def bs_loop(a, x):
  l = 0
  r = len(a) - 1

  while l <= r:
    m = (l + r) // 2

    if a[m] == x:
      return m
    elif x < a[m]:
      r = m - 1
    else:
      l = m + 1

  return -1

a = [2, 5, 8, 12, 16, 23, 38]

print(bs_loop(a, 16))
print(bs_loop(a, 7))
```

#### 再帰を使う方法

```python
def bs_rec(a, x):
  return bs_rec2(a, x, 0, len(a) - 1)


def bs_rec2(a, x, l, r):
  if l > r: return -1

  m = (l + r) // 2

  if a[m] == x: return m
  elif x < a[m]:
    return bs_rec2(a, x, l, m - 1)
  else:
    return bs_rec2(a, x, m + 1, r)


a = [2, 5, 8, 12, 16, 23, 38]

print(bs_rec(a, 16))
print(bs_rec(a, 7))
```


### 部分集合の列挙

#### 実用的な方法

練習

```python
from itertools import combinations

list(combinations([10, 20, 30], 2)) # [10, 20, 30]から2個選ぶ
```

本番

```python
from itertools import combinations

def subs_lib(a):
  for r in range(len(a) + 1):
    for s in combinations(a, r):
      print(list(s))


subs_lib([1, 2, 3])
```

#### 再帰を使う方法

```python
def subs_rec(a):
  subs_rec2(a, 0, [])


def subs_rec2(a, i, cur):
  if i == len(a):
    print(cur)
    return

  # a[i] を選ばない
  subs_rec2(a, i + 1, cur)

  # a[i] を選ぶ
  subs_rec2(a, i + 1, cur + [a[i]])


subs_rec([1, 2, 3])
```


### サイコロの合計の数え上げ

```python
n, q = 2, 7
#n, q = 9, 12 # 手法によって速度が異なる．① < ② < ③ < ④
```

#### ① 簡単な方法（product）

```python
from itertools import product

def count_product(n, q):
  count = 0

  for dice in product(range(1, 7), repeat=n):
    if sum(dice) == q:
      count += 1

  return count


print(count_product(n, q))
```

#### ② 再帰

```python
def count_rec(n, q):
  if n == 0:
    return 1 if q == 0 else 0

  count = 0

  for face in range(1, 7):
    count += count_rec(n - 1, q - face)

  return count


print(count_rec(n, q))
```

#### ③ 条件付再帰

```python
def count_cut(n, q):
  if n == 0:
    return 1 if q == 0 else 0

  if q < n:
    return 0

  if q > 6 * n:
    return 0

  count = 0

  for face in range(1, 7):
    count += count_cut(n - 1, q - face)

  return count


print(count_cut(n, q))
```

#### ♠④ メモ化

```python
from functools import cache

@cache
def count_memo(n, q):
  if n == 0:
    return 1 if q == 0 else 0

  if q < n:
    return 0

  if q > 6 * n:
    return 0

  count = 0

  for face in range(1, 7):
    count += count_memo(n - 1, q - face)

  return count


print(count_memo(n, q))
```

## プレゼン課題

### 1

非負整数 n の各桁の和を求める関数を示します．

```python
def sum_digits(n):
    return sum(int(ch) for ch in str(n))


print(sum_digits(5028))
print(sum_digits(123))
print(sum_digits(0))
```

この関数を，再帰を使って実装し，正しく動作することを確認してから，アルゴリズムを説明してください．



### 2

配列中の指定した値の個数を数える方法を示します．

```python
print([2, 3, 2, 5, 2].count(2))
print([1, 4, 4, 4, 7].count(4))
print([1, 2, 3].count(9))
```

配列中の指定した値の個数を数える関数を，再帰を使って実装してください．正しく動作することを確認してから，アルゴリズムを説明してください．



### 3

文字列が回文かどうかを判定する方法を示します．

```python
def is_palindrome(s):
    return s == s[::-1]


print(is_palindrome("level"))  # True
print(is_palindrome("apple"))  # False
```

文字列が回文かどうかを判定する関数を，再帰を使って実装してください．正しく動作することを確認してから，アルゴリズムを説明してください．



### 4

指定した長さの，A と B だけからなる文字列を全て出力する方法を示します．

```python
from itertools import product

def print_ab_strings(n):
    for chars in product("AB", repeat=n):
        print("".join(chars))


print_ab_strings(3)
```

指定した長さの，A と B だけからなる文字列を全て出力する関数を，再帰を使って実装してください．正しく動作することを確認してから，アルゴリズムを説明してください．
