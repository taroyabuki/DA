# 第10回 線形リスト（その1　単方向連結リスト）

## 講義資料

### 基本形と使用例

```python
class Node:
  def __init__(self, data, next=None):
    self.data = data
    self.next = next


class LinkedList:
  def __init__(self):
    self.head = None

  def add_first(self, data):
    self.head = Node(data, self.head)


# 使用例
a = LinkedList()
a.add_first(20)
a.add_first(10)
a.add_first(30)

print(a.head.data, end=' ')
print(a.head.next.data, end=' ')
print(a.head.next.next.data)

p = a.head
while p:
  print(p.data, end=' ')
  p = p.next
```

### 要素数を求める方法1

$O(n)$

```python
class Node:
  def __init__(self, data, next=None):
    self.data = data
    self.next = next


class LinkedList:
  def __init__(self):
    self.head = None

  def add_first(self, data):
    self.head = Node(data, self.head)
  
  def __len__(self):
    no = 0
    p = self.head
    while p:
      no += 1
      p = p.next
    return no


# 使用例
a = LinkedList()
a.add_first(20)
a.add_first(10)
a.add_first(30)
print(len(a))
# 3
```

### メモ

- 参考書List 8-1 [G]の `self.no -= 1` のインデントが間違っている．
- 参考書List 8-1 [J]の `remove_current_node()` には `if self.current is not None:` が必要．

### 要素数を求める方法2

$O(1)$ だが，要素数の管理が必要になる．

```python
class Node:
  def __init__(self, data, next=None):
    self.data = data
    self.next = next

class LinkedList:
  def __init__(self):
    self.head = None
    self.no = 0

  def add_first(self, data):
    self.head = Node(data, self.head)
    self.no += 1
  
  def __len__(self):
    return self.no


# 使用例
a = LinkedList()
a.add_first(20)
a.add_first(10)
a.add_first(30)
print(len(a))
# 3
```

## プレゼン課題

次の実装を使って解答してください．

```python
class Node:
  def __init__(self, data, next=None):
    self.data = data
    self.next = next

class LinkedList:
  def __init__(self):
    self.head = None

  def add_first(self, data):
    self.head = Node(data, self.head)
```

### 1

単方向連結リストにおいて，指定したデータを持つ要素を探索する方法を説明し，メソッド `find` を実装してください．`find` は要素が存在するならその要素を返し，存在しないなら None を返すようにしてください．計算量についても説明してください．

### 2

単方向連結リストにおいて，指定したデータを持つ要素が存在するかどうかを調べる方法を説明して，メソッドとして実装してください．計算量についても説明してください．1で定義するメソッド `find` を活用してもかまいません．メソッド名を `__contains__` にすると，演算子 `in` が使えるようになって便利です．使用例を示します．

### 3

単方向連結リストにおいて，指定したデータ `data` をもつ要素の次にデータ `new_data` をもつ新しい要素を挿入する方法を説明し，メソッド `insert` で実装してください．計算量についても説明してください．1で定義するメソッド `find` を活用してもかまいません．

### 4

単方向連結リストにおいて，指定したデータ `data` をもつ要素を前から探索して最初に見つかったものを削除する方法を説明し，メソッド`remove` で実装してください．計算量についても説明してください．

### 5

実行結果が使用例に合うようにする方法を説明して，実装してください．

```python
# 使用例
a = LinkedList()
a.add_first(20)
a.add_first(10)
a.add_first(30)

for x in a:
  print(x, end=", ")
# 30, 10, 20, 
```

